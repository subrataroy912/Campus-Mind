import PageShell from '../PageShell.jsx'

function LoginPage() {
  return (
    <PageShell
      description="Authenticate teachers and students before they enter the classroom workspace."
      eyebrow="Authentication"
      title="Login"
    />
  )
}

export default LoginPage
